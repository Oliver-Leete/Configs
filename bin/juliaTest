#!/bin/bash
project="$(basename "$PWD")Tests.jl"
cd test
# julia --project -i -e "include(\"$project\")"
julia --threads=16 --project -i -e 'using Revise, ReTest, Debugger, Infiltrator; ReTest.load("'"$project"'", revise=true)'
