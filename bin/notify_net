#!/bin/bash

dunstify -r 103 "$(nmcli -t device | rg -e "[^(dis)]connected" | sed -E -e 's/.*(wifi|ethernet).*:([^:])/\1: \2/')"

# if [[ "$net_stat" == *"ethernet"*"connected"* ]]; then
#     connections=$(echo $net_stat | rg -e "connected")
# elif [[ "$net_stat" == *"wifi"*"connected"* ]]; then
#     net_icon=""
# else
#     net_icon="睊"
# fi
