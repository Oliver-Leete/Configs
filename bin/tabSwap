#!/bin/bash

tabcount=$(kitty @ ls | jq ".[] | select(.is_focused) | .tabs[].id" | wc -l)

if [ "$tabcount" -gt 1 ]; then
	xdotool key --clearmodifiers ctrl+alt+$1
else
	xdotool key super+shift+meta+$2
fi

